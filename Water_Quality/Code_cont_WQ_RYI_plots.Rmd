---
title: "Code_cont_WQ_RYI_plots"
author: "Amanda Maguire"
date: "July 27, 2020"
output: 
  html_document: 
    code_folding: show
    toc: yes
    toc_float:
      collapsed: no
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

# Purpose

Code to plot continuous water quality data and check for missing data during the study period.

```{r load packages, message = FALSE, warning = FALSE}
# Load packages
library(tidyverse)
library(lubridate)
```

# Import Data

```{r import data}
# Define path on SharePoint site for data
sharepoint_path <- normalizePath(
  file.path(
    Sys.getenv("USERPROFILE"),
    "California Department of Water Resources/Office of Water Quality and Estuarine Ecology - North Delta Flow Action/WQ_Subteam/Processed_Data/Continuous"
  )
)

# Import RYI data
ryi_orig <- 
  read_csv(
    file = paste0(sharepoint_path, "/RTM_OUTPUT_RYI_formatted.csv"),
    col_types = paste0("c","T","c", str_c(rep("d", 24), collapse = ""))
  )
```

Now, the data looks as we expect it to:
```{r glimpse data}
glimpse(ryi_orig)
```

# Create Plots

## One parameter

Simple line plot of the parameters collected at the RYI station. We'll use the `ggplot` function from the ggplot2 package.

pH plot
```{r plot one param pH, warning = FALSE}
ph <- ryi_orig %>% 
  ggplot(aes(x = DateTime, y = pH)) +
  geom_line()

print(ph)
```

Focus in on pH data
```{r plot one param pH change x axis lim, warning = FALSE}
ph + scale_x_datetime(limits = c(as_datetime("2017-06-01 00:00:00"), NA))
```

SpCnd plot
```{r plot one param SpCnd, warning = FALSE}
ps <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = SpCnd)) +
  geom_line()

print(ps)
```

Getting a closer look at SpCnd data
```{r plot one param SpCnd change x axis lim, warning = FALSE}
ps + scale_x_datetime(limits = c(as_datetime("2017-06-01 00:00:00"), NA))
```

Turbidity plot
```{r plot one param Turbidity, warning = FALSE}
pt <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = Turbidity)) +
  geom_line()

print(pt)
```

Edit y axis to zoom Turbidity.
```{r plot one param Turbidity change x axis lim, warning = FALSE}
pt + ylim(NA,200)
```

DO plot
```{r plot one param DO, warning = FALSE}
pd <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = DO)) +
  geom_line()

print(pd)
```

Edit x axis to zoom DO data
```{r}
pd + scale_x_datetime(limits = c(as_datetime("2017-01-01 00:00:00"), NA))
```

WaterTemp plot
```{r plot one param WaterTemp, warning = FALSE}
p <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = WaterTemp)) +
  geom_line()

print(p)
```

Chla plot
```{r plot one param Chla, warning = FALSE}
pc <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = Chla)) +
  geom_line()

print(pc)
```

Edit x axis to zoom Chla data
```{r}
pc + scale_x_datetime(limits = c(as_datetime("2019-06-01 00:00:00"), NA))
```

Chla_RFU plot
```{r}
pcr <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = Chla_RFU)) +
  geom_line()

print(pcr)
```

Edit x axis to zoom Chla_RFU data
```{r}
pcr + scale_x_datetime(limits = c(as_datetime("2017-01-01 00:00:00"), NA))
```

fDOM plot
```{r}
pf <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = fDOM)) +
  geom_line()

print(pf)
```

Edit x axis and y axis to zoom fDOM data
```{r}
pf + scale_x_datetime(limits = c(as_datetime("2017-03-01 00:00:00"), NA)) +
  ylim(NA, 55)
```

Phyco_RFU plot
```{r}
pbg <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = Phyco_RFU)) +
  geom_line()

print(pbg)
```

Zoom in on Phyco_RFU data
```{r}
pbg + scale_x_datetime(limits = c(as_datetime("2019-04-01 00:00:00"), NA))
```

NitrateNitrite plot
```{r}
pn <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = NitrateNitrite)) +
  geom_line()

print(pn)
```

Zoom in on NitrateNitrite data
```{r}
pn + scale_x_datetime(limits = c(as_datetime("2018-12-01 00:00:00"), NA)) +
  ylim(NA, 1.0)
```

Flow plot
```{r}
pflo <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = Flow)) +
  geom_line()

print(pflo)
```

FlowTF plot
```{r}
pft <- ryi_orig %>%
  ggplot(aes(x = DateTime, y = FlowTF)) +
  geom_line()

print(pft)
```

Edit x axis to zoom FlowTF data
```{r}
pft + scale_x_datetime(limits =  c(as_datetime("2018-06-01 00:00:00"), as_datetime("2019-07-01 00:00:00"))) +
  ylim(NA, 125000)
```

## Multiple parameters

This is nice, but we can also plot multiple parameters at once on a single plot with `facet_grid`. But, first we have to remove the Quality Code variables and restructure the data to a long format using `pivot_longer`.
```{r pivot data longer}
ryi_long <- ryi_orig %>% 
  select(!ends_with("_Qual")) %>% 
  pivot_longer(
    cols = Flow:NitrateNitrite,
    names_to = "parameter",
    values_to = "value"
  )
```

Now the data is structured so that the parameter names are in the `parameter` variable and the data values are in the `value` variable. 
```{r display data in long format}
head(ryi_long)
```

Let's create a plot with facets. Use the `scales` argument set to `"free_y"` in `facet_grid` to adjust y-axis limits to the data of each parameter.
```{r plot with facets, warning = FALSE}
p2 <- ryi_long %>%
  ggplot(aes(x = DateTime, y = value)) +
  geom_line() +
  facet_grid(
    rows = vars(parameter),
    scales = "free_y"
  )

p2
```

This is nice, but it would be even nicer to make the plot larger so we can see it better. This is done with the `fig.width` and `fig.height` options in the top of the code chunk.
```{r plot with facets larger, fig.width = 8.5, fig.height = 11}
p2
```

Zoom 2011-2014
```{r plot with facets change x axis lim 2011, warning = FALSE, fig.height = 8}
p2 + scale_x_datetime(
  limits = c(
    as_datetime("2011-01-01 00:00:00"), 
    as_datetime("2014-01-01 00:00:00")
  )
)
```

Zoom 2017-2019
```{r plot with facets change x axis lim 2017, warning = FALSE, fig.height = 8}
p2 + scale_x_datetime(
  limits = c(
    as_datetime("2017-06-01 00:00:00"), 
    as_datetime("2019-12-01 00:00:00")
  )
)
```

